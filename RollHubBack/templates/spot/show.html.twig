{% extends 'base.html.twig' %}

{% block title %}Spot{% endblock %}

{% block body %}
    <h1>Spot</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ spot.id }}</td>
            </tr>
            <tr>
                <th>Latitude</th>
                <td>{{ spot.latitude }}</td>
            </tr>
            <tr>
                <th>Longitude</th>
                <td>{{ spot.longitude }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ spot.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_spot_index') }}">back to list</a>

    <a href="{{ path('app_spot_edit', {'id': spot.id}) }}">edit</a>

    {{ include('spot/_delete_form.html.twig') }}
{% endblock %}
