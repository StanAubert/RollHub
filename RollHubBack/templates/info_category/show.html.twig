{% extends 'base.html.twig' %}

{% block title %}InfoCategory{% endblock %}

{% block body %}
    <h1>InfoCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ info_category.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ info_category.title }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ info_category.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_info_category_index') }}">back to list</a>

    <a href="{{ path('app_info_category_edit', {'id': info_category.id}) }}">edit</a>

    {{ include('info_category/_delete_form.html.twig') }}
{% endblock %}
